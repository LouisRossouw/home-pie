{
  "name": "home-pie",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "desktop": "pnpm run dev --filter=home-pie-desktop",
    "server-host": "start-server-host.bat",
    "server-local": "start-server-local.bat"
  },
  "devDependencies": {
    "@pie/typescript-config": "workspace:*",
    "prettier": "^3.5.0",
    "turbo": "^2.4.4",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "tailwind-config": "^0.1.2"
  }
}
